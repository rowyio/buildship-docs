import nodes from './mailchimp/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# MailChimp

Easily integrate MailChimp into your BuildShip workflows using pre-defined nodes. Automate your email marketing, manage
subscriber lists, and track campaign performance seamlessly, enabling efficient and effective communication with your
audience directly from your workflows.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/mailchimp/${node.meta.id}`}
    />
  ))}
</Cards>
