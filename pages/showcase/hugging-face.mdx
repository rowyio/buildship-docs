import nodes from './hugging-face/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Hugging Face

BuildShip offers pre-configured nodes for integrating Hugging Face's popular models into your workflows. Effortlessly
leverage state-of-the-art NLP and machine learning models for tasks such as text classification, translation,
summarization, and more. Streamline model deployment and interaction to enhance your automation and data analysis
capabilities.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/hugging-face/${node.meta.id}`}
    />
  ))}
</Cards>
