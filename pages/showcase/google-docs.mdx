import nodes from './google-docs/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Google Docs

BuildShip offers OAuth Nodes specifically designed for integrating with the Google Docs API, enabling seamless
interaction with your Google Docs files directly within your workflows. Utilize these nodes to read, write, update, and
manage your documents efficiently, automating repetitive tasks and enhancing productivity.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/google-docs/${node.meta.id}`}
    />
  ))}
</Cards>
