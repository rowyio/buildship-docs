import nodes from './medium/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Medium

BuildShip offers pre-defined nodes for seamless Medium integration, allowing you to publish and manage blog content
within your workflows effortlessly. Automate your content distribution, handle authentication, and streamline your
blogging process with ease.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/medium/${node.meta.id}`}
    />
  ))}
</Cards>
