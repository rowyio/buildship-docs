import nodes from './webflow/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Webflow

BuildShip provides pre-defined nodes for seamless interaction with your Webflow projects. Effortlessly manage assets,
forms, and collections within your workflows, allowing you to automate tasks, sync data, and enhance your Webflow
integrations with ease.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/webflow/${node.meta.id}`}
    />
  ))}
</Cards>
