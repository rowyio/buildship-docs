import nodes from './bing/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Bing

BuildShip provides pre-defined nodes for integrating Microsoft Bing services, enabling comprehensive web search
functionalities within your workflows. Access Bing’s powerful search engine to extract relevant information, perform
data scraping, and integrate search capabilities into your custom applications seamlessly.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/bing/${node.meta.id}`}
    />
  ))}
</Cards>
