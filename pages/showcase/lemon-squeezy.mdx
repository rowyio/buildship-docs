import nodes from './lemon-squeezy/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Lemon Squeezy

BuildShip offers seamless integration with Lemon Squeezy, allowing you to connect with your Lemon Squeezy accounts and
webhooks effortlessly. Automate tasks such as managing products, handling customer data, and receiving real-time updates
on transactions, resigning manual interactions for streamlined workflows.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/lemon-squeezy/${node.meta.id}`}
    />
  ))}
</Cards>
