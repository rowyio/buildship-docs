import nodes from './signnow/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# SignNow

BuildShip provides nodes for SignNow integration, enabling streamlined sending and processing of electronic signatures
within your automated workflows. Handle document preparation, signature requests, and tracking effortlessly, ensuring
secure and efficient electronic documentation processes.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/signnow/${node.meta.id}`}
    />
  ))}
</Cards>
