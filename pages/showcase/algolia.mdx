import nodes from './algolia/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Algolia

BuildShip offers nodes to seamlessly integrate Algolia's powerful search capabilities into your workflows. Easily
perform high-speed, relevant searches, manage indexes, and enhance your user experience with minimal configuration.
Leverage Algolia's advanced search algorithms to deliver real-time results and improve data retrieval efficiency.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/algolia/${node.meta.id}`}
    />
  ))}
</Cards>
