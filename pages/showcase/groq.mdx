import nodes from './groq/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Groq

Nodes enable seamless integration with Groq's LPU Inference Engine for ultra-fast computations, facilitating advanced
real-time data processing and accelerating AI/ML workloads. Use these nodes to optimize performance, efficiency, and
scalability in your machine learning and data-rich applications.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/groq/${node.meta.id}`}
    />
  ))}
</Cards>
