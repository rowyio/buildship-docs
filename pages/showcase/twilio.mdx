import nodes from './twilio/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Twilio

BuildShip offers comprehensive nodes for Twilio integration, enabling seamless interaction with Twilio's messaging API.
Effortlessly send and receive SMS and MMS, manage communication workflows, and automate message handling, enhancing your
messaging capabilities within your applications.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/twilio/${node.meta.id}`}
    />
  ))}
</Cards>
