import nodes from './cohere/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Cohere

Utilize Cohere's pre-built nodes to seamlessly harness powerful Large Language Models (LLMs) via the Cohere API. Perform
text generation, classification, and embedding tasks with ease, accelerating your AI and natural language processing
projects and workflows efficiently.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/cohere/${node.meta.id}`}
    />
  ))}
</Cards>
