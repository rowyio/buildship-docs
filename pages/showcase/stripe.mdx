import nodes from './stripe/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Stripe

BuildShip provides pre-defined nodes for integrating with Stripe, enabling you to easily handle Stripe events within
your workflows. Seamlessly manage payments, subscriptions, customer data, and more, with built-in functionality to
process and react to various Stripe event types.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/stripe/${node.meta.id}`}
    />
  ))}
</Cards>
