import nodes from './firestore/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Firestore

BuildShip offers pre-defined nodes to interact with Firebase Firestore, enabling seamless integration within your
workflows. Easily perform a variety of CRUD operations on your Firestore databases, manage collections, and automate
data handling tasks.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/firestore/${node.meta.id}`}
    />
  ))}
</Cards>
