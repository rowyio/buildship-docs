import nodes from './sendgrid/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Sendgrid

BuildShip provides nodes to easily integrate with SendGrid for sending emails within your workflows. Automate your email
communications, manage templates, track delivery rates, and handle transactional and marketing email campaigns
seamlessly.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/sendgrid/${node.meta.id}`}
    />
  ))}
</Cards>
