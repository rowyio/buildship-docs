import nodes from './loops/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Loops

BuildShip simplifies email campaign management and transactional communication with pre-configured nodes for Loops.
Seamlessly integrate with the Loops email platform to send marketing and transactional emails, manage subscriber lists,
and track performance analytics, enhancing your email outreach efficiency and effectiveness.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/loops/${node.meta.id}`}
    />
  ))}
</Cards>
