import nodes from './pinecone/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Pinecone

BuildShip offers pre-defined nodes for seamless integration with the Pinecone vector database. This enables users to
efficiently interact with high-dimensional vector data, allowing for indexing, querying, and managing your vector-based
machine learning models within your workflows.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/pinecone/${node.meta.id}`}
    />
  ))}
</Cards>
