import nodes from './dev/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# DEV

BuildShip offers pre-configured nodes for publishing and managing blog posts on dev.to. Seamlessly interface with the
dev.to API to create, update, and delete articles, enhancing your blogging workflow by automating content management
tasks for efficient publishing and maintenance.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/dev/${node.meta.id}`}
    />
  ))}
</Cards>
