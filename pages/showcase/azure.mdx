import nodes from './azure/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Azure

Leverage Microsoft Azure's comprehensive set of AI models directly within your workflows using pre-defined nodes. These
integrations facilitate easy access to Azure's cognitive services, enabling tasks such as natural language processing,
image recognition, and machine learning model deployment. Seamlessly enhance your applications with Azure's robust AI
capabilities.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/azure/${node.meta.id}`}
    />
  ))}
</Cards>
