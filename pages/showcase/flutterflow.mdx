import nodes from './flutterflow/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# FlutterFlow

BuildShip offers comprehensive nodes that integrate directly with FlutterFlow, enabling smooth interactions with your
frontend applications built on the platform. Utilize these nodes to automate tasks, streamline workflows, and enhance
the functionality within your FlutterFlow projects.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/flutterflow/${node.meta.id}`}
    />
  ))}
</Cards>
