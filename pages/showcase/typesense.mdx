import nodes from './typesense/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Typesense

BuildShip offers a set of nodes designed to leverage Typesense functionality, enabling quick and efficient real-time
search and index management within your workflow. Easily integrate powerful full-text search capabilities into your
applications, automate data indexing, and enhance search performance using this intuitive interface.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/typesense/${node.meta.id}`}
    />
  ))}
</Cards>
