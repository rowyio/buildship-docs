import nodes from './aws-s3-storage/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# AWS S3 Storage

BuildShip provides versatile nodes for seamless interaction with AWS S3 Storage, enabling you to manage your S3 buckets
efficiently. Perform operations such as creating, listing, and deleting buckets, uploading and downloading files, and
managing bucket properties within your workflows. Experience robust cloud storage integration without the need for
complex coding.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/aws-s3-storage/${node.meta.id}`}
    />
  ))}
</Cards>
