import nodes from './google-maps/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Google Maps

BuildShip offers nodes that integrate with the Google Maps API, allowing you to seamlessly perform various actions such
as geocoding addresses, retrieving place details, calculating distances, and generating maps. Effortlessly incorporate
robust mapping functionalities into your workflows for enhanced geospatial data handling and location-based services.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/google-maps/${node.meta.id}`}
    />
  ))}
</Cards>
