import nodes from './google-translate/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Google Translate

BuildShip provides pre-defined nodes for Google Translate, enabling seamless integration with the Google Translate API.
Effortlessly perform language translations within your workflows, supporting a wide range of languages, and automate
text conversion processes to suit your multilingual needs.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/google-translate/${node.meta.id}`}
    />
  ))}
</Cards>
