import nodes from './ffmpeg/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# FFmpeg

Nodes for FFmpeg integration facilitate powerful video and audio processing automation within your workflows. Utilize
FFmpeg's extensive functionalities to transcode, convert, edit, and stream multimedia content efficiently. Simplify
complex media manipulation tasks with pre-configured nodes tailored for FFmpeg operations, enabling streamlined media
workflows and seamless integration into your applications.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/ffmpeg/${node.meta.id}`}
    />
  ))}
</Cards>
