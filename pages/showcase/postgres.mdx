import nodes from './postgres/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Postgres

BuildShip enables seamless integration with PostgreSQL databases through pre-defined nodes, allowing you to perform
complex queries, manage schemas, and execute CRUD operations efficiently. Enhance your workflows with robust database
interactions, automate tasks, and streamline data management without extensive coding.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/postgres/${node.meta.id}`}
    />
  ))}
</Cards>
