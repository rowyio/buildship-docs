import nodes from './document-ai/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Document AI

BuildShip provides pre-defined nodes for Google Cloud Document AI, allowing for seamless integration of advanced
document processing capabilities within your workflows. Effortlessly extract and process data from scanned documents,
PDFs, and images, enhancing automation and improving accuracy in data handling and extraction tasks.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/document-ai/${node.meta.id}`}
    />
  ))}
</Cards>
