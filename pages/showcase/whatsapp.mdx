import nodes from './whatsapp/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# WhatsApp

BuildShip offers pre-defined nodes for integrating with WhatsApp by Meta, enabling seamless interaction within your
automation workflows. Utilize these nodes to send and receive messages, manage contacts, create chatbots, and streamline
communication processes on the WhatsApp platform.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/whatsapp/${node.meta.id}`}
    />
  ))}
</Cards>
