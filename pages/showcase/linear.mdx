import nodes from './linear/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Linear

BuildShip offers pre-defined nodes to seamlessly integrate with Linear, enabling you to automate tasks and manage
projects more efficiently within your workflows. Utilize these nodes to create, update, and manage issues, projects, and
related entities in Linear, enhancing productivity and streamlining project management tasks.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/linear/${node.meta.id}`}
    />
  ))}
</Cards>
