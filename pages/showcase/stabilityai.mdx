import nodes from './stabilityai/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# StabilityAI

Seamlessly integrate StabilityAI's advanced machine learning models with BuildShip's pre-defined nodes. This integration
allows you to leverage StabilityAI’s capabilities for various complex AI tasks such as data analysis, predictive
modeling, and automation within your workflows, enhancing efficiency and accuracy.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/stabilityai/${node.meta.id}`}
    />
  ))}
</Cards>
