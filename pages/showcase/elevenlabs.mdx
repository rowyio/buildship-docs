import nodes from './elevenlabs/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# ElevenLabs

Leverage ElevenLabs Text to Speech capabilities within your projects using pre-built nodes that streamline interaction.
Integrate high-quality, natural-sounding speech synthesis into your workflows effortlessly, enabling a range of
applications from automated announcements to personalized audio content generation.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/elevenlabs/${node.meta.id}`}
    />
  ))}
</Cards>
