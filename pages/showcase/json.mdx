import nodes from './json/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# JSON

Provide predefined nodes for JSON validation and transformation, enabling you to efficiently validate JSON structures
and transform JSON data into the desired format. Streamline your processes with automated JSON manipulation, ensuring
data integrity and flexibility in your workflows.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/json/${node.meta.id}`}
    />
  ))}
</Cards>
