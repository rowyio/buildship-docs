import nodes from './google-cloud/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Google Cloud

BuildShip provides pre-defined nodes for seamless integration with Google Cloud Platform services. This allows users to
interact with various GCP offerings, such as Google Compute Engine, Cloud Storage, BigQuery, and more. Perform tasks
like data analysis, storage management, and scalable computing within your workflows effortlessly.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/google-cloud/${node.meta.id}`}
    />
  ))}
</Cards>
