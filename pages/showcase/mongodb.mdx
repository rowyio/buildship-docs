import nodes from './mongodb/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# MongoDB

BuildShip provides pre-defined nodes for MongoDB integration, enabling easy interaction with MongoDB databases within
your workflows. Perform a variety of database operations, including CRUD operations, data aggregation, indexing, and
querying, seamlessly within your automation processes.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/mongodb/${node.meta.id}`}
    />
  ))}
</Cards>
