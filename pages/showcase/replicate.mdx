import nodes from './replicate/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Replicate

Replicate offers pre-defined nodes for integrating and utilizing hosted open-source machine learning models within your
workflows. Easily run and manage a wide range of models for tasks such as image generation, text analysis, and more,
making AI capabilities accessible without extensive coding.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/replicate/${node.meta.id}`}
    />
  ))}
</Cards>
