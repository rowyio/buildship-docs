import nodes from './youtube/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# YouTube

BuildShip provides pre-defined nodes for YouTube integration, enabling users to perform various actions using YouTube
content. Easily manage YouTube videos and playlists, fetch data analytics, upload new content, automate video processing
workflows, and more within your projects.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/youtube/${node.meta.id}`}
    />
  ))}
</Cards>
