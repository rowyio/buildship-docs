import nodes from './discord/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Discord

BuildShip offers pre-configured nodes for seamless integration with Discord, enabling you to automate and manage various
Discord activities within your workflows. Easily send messages, manage channels, and interact with your community
directly from your applications, leveraging Discord's extensive range of functionalities.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/discord/${node.meta.id}`}
    />
  ))}
</Cards>
