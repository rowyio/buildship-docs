import nodes from './google-calendar/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Google Calendar

Utilize BuildShip's OAuth nodes to seamlessly integrate with the Google Calendar API. By leveraging these nodes, you can
effortlessly retrieve, create, update, and delete events, allowing for dynamic scheduling, automation, and calendar
management within your workflows.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/google-calendar/${node.meta.id}`}
    />
  ))}
</Cards>
