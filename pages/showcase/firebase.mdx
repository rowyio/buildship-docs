import nodes from './firebase/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Firebase

BuildShip provides pre-defined nodes for Firestore integration, making it easy to interact with Firebase services within
your workflows. Interact seamlessly with your own Firebase projects, performing a range of CRUD operations, building
APIs, automations, Cron Jobs, and more.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/firebase/${node.meta.id}`}
    />
  ))}
</Cards>
