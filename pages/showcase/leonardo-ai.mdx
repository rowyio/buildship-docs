import nodes from './leonardo-ai/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Leonardo AI

BuildShip offers pre-configured nodes to effortlessly integrate with Leonardo AI services and models, enabling seamless
interaction within your workflows. Utilize Leonardo AI's advanced capabilities to deploy, monitor, and manage machine
learning models, automate data-driven processes, and perform complex analytical tasks efficiently.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/leonardo-ai/${node.meta.id}`}
    />
  ))}
</Cards>
