import nodes from './slack/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Slack

The Slack integration allows you to effortlessly send messages to your Slack channels and direct messages from your
workflows. Ideal for automating notifications, alerts, and updates to team members, this integration enhances
collaboration and ensures that your team stays informed in real-time.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/slack/${node.meta.id}`}
    />
  ))}
</Cards>
