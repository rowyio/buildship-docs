import nodes from './resend/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Resend

BuildShip provides pre-defined nodes for integrating with Resend, making it simple to incorporate email sending
capabilities into your workflows. Effortlessly dispatch emails through Resend using customizable parameters, and
automate your communication processes with minimal setup required.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/resend/${node.meta.id}`}
    />
  ))}
</Cards>
