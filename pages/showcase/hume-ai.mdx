import nodes from './hume-ai/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Hume AI

BuildShip provides predefined nodes for seamless interaction with Hume AI’s APIs. Harness Hume AI's advanced emotional
intelligence technology to analyze and interpret emotional signals in text, audio, and video, integrating these insights
directly into your workflows to enhance user experiences and decision-making processes.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/hume-ai/${node.meta.id}`}
    />
  ))}
</Cards>
