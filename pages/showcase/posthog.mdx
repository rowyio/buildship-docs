import nodes from './posthog/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Posthog

BuildShip offers pre-configured nodes for integrating with Posthog, enabling seamless capture and transmission of
analytics data within your workflows. Utilize these nodes to track user behavior, generate insights, and automate your
analytics processes efficiently.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/posthog/${node.meta.id}`}
    />
  ))}
</Cards>
