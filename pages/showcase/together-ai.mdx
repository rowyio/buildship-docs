import nodes from './together-ai/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# TogetherAI

Utilize pre-defined nodes to seamlessly interact with TogetherAI, enabling the execution of leading open-source models
such as Llama-3. Integrate these AI models into your workflows to run advanced AI computations, build intelligent
applications, automate tasks, and enhance your project capabilities with ease.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/together-ai/${node.meta.id}`}
    />
  ))}
</Cards>
