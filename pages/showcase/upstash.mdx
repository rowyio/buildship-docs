import nodes from './upstash/nodes.json';
import { Card, Cards } from 'nextra/components';
import NodeCard from '/components/nodeCard';

# Upstash

BuildShip provides pre-defined nodes for Upstash integration, enabling effortless interaction with your Upstash Redis
database within your workflows. Seamlessly leverage the `@upstash/redis` npm package to perform a variety of database
operations, API integrations, automations, and more, streamlining your Redis management.

## Nodes

<Cards num={3}>
  {nodes.map((node) => (
    <NodeCard
      key={node.meta.id}
      title={node.meta.name}
      image={node.meta.icon.url}
      href={`/showcase/upstash/${node.meta.id}`}
    />
  ))}
</Cards>
