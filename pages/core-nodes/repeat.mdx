# Repeat

import { Callout } from 'nextra/components'
import Image from 'next/image'
import repeat from '/public/nodes/repeat.png'

Repeat node is similar to loop node however you can repeat/loop over other nodes x number of times. You can mention how many times you want to reapeat a task and add all the logic nodes inside the repeat node.

<Image src={repeat} alt="Repeat node" width={1200} />